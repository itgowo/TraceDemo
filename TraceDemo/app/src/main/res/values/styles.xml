<resources>
    <style name="AppTheme" parent="Theme.AppCompat.NoActionBar">
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowActionBar">false</item>
        <item name="android:fitsSystemWindows">true</item>
        <item name="android:inAnimation">@null</item>
        <item name="android:outAnimation">@null</item>
        <item name="android:screenOrientation">portrait</item>

        <!-- toolbar（actionbar）颜色 -->
        <item name="colorPrimary">@color/ActivityBackgroundColorWhite</item>
        <!-- 状态栏颜色 -->
        <item name="colorPrimaryDark">@color/v180_text_1</item>
        <!-- 窗口的背景颜色 -->
        <item name="android:windowBackground">@android:color/transparent</item>
        <!-- SearchView -->
    </style>
    <style name="WaitDialog" parent="Theme.AppCompat.Light.Dialog.MinWidth">
        <item name="android:windowFrame">@null</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowBackground">@android:color/transparent
        </item>    <!-- dialog应该是透明背景 -->
        <item name="android:windowIsTranslucent">false</item>
    </style>
</resources>
